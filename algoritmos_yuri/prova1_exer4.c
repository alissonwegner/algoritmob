#include<stdio.h>
#include<stdlib.h>
func (char *i){
  int *b;
  int valor;
  b = str;
    for ( ;*a;a++) {
      for (b = a+1;*b && *b!=a :b++){
        if (!(*b))
      }
  /* code */
}

}
main(){
  char str[50];
  gets(str);
numero = func (str);


}
